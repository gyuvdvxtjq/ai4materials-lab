# 面试讲解卡 v2（优化后数字 · 投递后面试前吃透）

## 0 · MatBench 官方对标（新增，最重要的锚点）

**一句话**：在 MatBench 官方数据 + 官方 5 折划分上实测了本项目基线，指标从此「可对标」。

| 任务 | 本项目 | 官方 RF-SCM/Magpie | 官方 MODNet |
|---|---:|---:|---:|
| expt_gap（MAE↓） | **0.4459 ± 0.0182** | 0.4461 | 0.3327 |
| expt_is_metal（ROC-AUC↑） | **0.9721 ± 0.0024** | 0.9356 | 0.9739 |

**必讲的两点**：
1. 回归与官方 RF 基线**一字不差**（0.4459 vs 0.4461）——证明管线正确，也证明「magpie+RF 就是组分基线的水平上限」。
2. 分类 0.972 **大幅超过**官方 RF（0.936）、几乎追平 MODNet（0.974）——magpie 对「金属性判别」的分辨率远高于对「带隙数值」的分辨率，与 P1 拆分实验互相印证。

## 0.5 · CrabNet 与修正版 CGCNN（新增，两条硬结论）

| 实验 | 结果 | 一句话结论 |
|---|---|---|
| CrabNet（缩减版，官方 5 折） | **0.3953 ± 0.0118** vs RF 0.4459 | 同为纯组分，换对模型比调参有效（-11% vs RF 调参仅 -2%） |
| 修正版 CGCNN（同数据同划分） | **0.574** vs RF 0.593 / 旧 GNN 0.594 | 旧版「GNN 打平 RF」一半是架构偏差的锅；对齐原作后 GNN 稳定胜出 |

**CrabNet 必讲**：用的是缩减版配置（d_model=128 vs 原作 512、40 epochs、无 SWA），是**下限**；官方完整版 0.346。顺带展示了我踩平了 crabnet 1.0.1 的 5 个环境坑（包名 bug/缺数据文件/优化器 hook/DataLoader 死锁/BLAS 死锁），补丁全部固化在 `benchmark/run_crabnet.py`。

**CGCNN 必讲**：5 项对齐原作的修正——one-hot 元素嵌入、BatchNorm、softplus、sum pooling、12 最近邻（旧版 5Å 全邻居平均 601 边/图 → 169 边，提速 3.6 倍）。逐项能说出「为什么原作这么设计」。

---

## P1 v3 · 金属/半导体分类 + 带隙回归拆分（新增，更重要）

**一句话**：v2 的 R²=0.714 是「虚高」的——60% 样本带隙为 0，回归器只需认出金属。v3 把问题拆成两个诚实的问题：

| 任务 | 指标（5 折 CV，mean±std） |
|---|---:|
| metal/non-metal 分类 | ROC-AUC **0.952 ± 0.006** |
| 非金属子集带隙回归 | MAE **0.522 ± 0.012** eV（HGB 调参后） |
| 全量回归（参考） | MAE 0.337，R² 0.689（60.4% 是金属） |

**为什么这是加分项（必讲）**：
1. 「为什么不直接报 R²=0.714？」→ 那是个被 60% 零带隙样本灌出来的数字，**把『识别金属』和『预测带隙数值』混在一起了**。MatBench 官方也把 `is_metal`（分类）和 `gap`（回归）拆成两个独立任务——这是领域的标准做法，不是我发明的。
2. 「分类为什么这么强（AUC 0.95）？」→ 金属性主要由组成决定（有无过渡金属、电负性分布），magpie 组分特征天然能抓；这正好和 P3 里「硫化物金属性被误判」呼应——**组分模型擅长『金属/非金属』，但不擅长『带隙具体数值』**。
3. 「非金属子集 MAE 0.52 是不是比 0.324 变差了？」→ 不是变差，是**更诚实**。0.324 里大部分是「预测金属≈0」的免费分；0.52 才是半导体/绝缘体带隙的真实难度。
4. 「你调参了吗？调参有用吗？」→ 调了（对 HGB 网格搜索），但**收益很小**：分类 0.9504→0.9514，回归 0.533→0.522。这说明**瓶颈是组分特征，不是模型超参**——这是比「我会调参」更高级的认知，评审就喜欢听这种「知道该在哪里使劲」的判断。
5. 「为什么输出不确定度？」→ 随机森林树间标准差（≈0.745 eV），给 P3 筛选时按「预测落在目标窗口的概率」排序，而不是点估计。

---

## P1 · 带隙预测（MAE=0.324 eV, R²=0.714）

**一句话**：用材料信息学标准描述符（magpie）+ 梯度提升，在 6772 条真实 DFT 数据上把带隙预测 R² 做到 0.71。

**迭代故事（最值钱的部分）**：
- v1（35维手写特征, 2726条）：R²=0.592
- v2（66维, 3511条）：R²=0.640
- v3（66维, 6772条）：R²=0.590 ← **数据翻倍指标反降**
- v4（**magpie 132维**, 6772条）：R²=**0.714**

**必问问题**：
1. "为什么 v3 数据更多反而更差？" → skip 深分页拉到的化学体系更杂（更多非氧化物/复杂三元体系），**数据分布比数据量更关键**——这是数据集工程的真实教训。
2. "magpie 是什么？" → 材料信息学标准元素属性描述符集（Ward et al. 2016，nat. comm.），涵盖原子量/电负性/电离能/价电子/原子半径/熔沸点/密度等 20+ 属性的统计量。用 matminer 库加载。
3. "为什么不自己手写特征了？" → v1-v3 手写特征已到天花板（R²~0.6），换标准描述符一步跃升 0.12——**知道什么时候用现成的领域知识，比重复造轮子更重要**。

**模型选择**：训练脚本先用 validation MAE 选择 RF 或 HistGradientBoosting，再在 train+validation 上重训，最后只报告一次 test 指标；不要把模型名称写死。

---

## P2 · GNN 对比研究（v3 修正后：CGCNN 0.574 vs RF 0.593，GNN 胜出）

**一句话**：通过 4 轮迭代（架构升级 + 数据扩充 5.5 倍），把晶体图神经网络与组分特征基线的差距从 0.061 缩到 0.003 eV，验证了"结构信息价值随数据规模兑现"。

**迭代轨迹**：
| 轮 | 架构 | n | GNN MAE | 差距 |
|---|---|---|---|---|
| 1 | GCN（无距离信息） | 364 | 0.755 | -0.061 |
| 2 | GCN | 1347 | 0.606 | -0.008 |
| 3 | **距离感知消息传递** | 1347 | 0.654 | -0.056 |
| 4 | 距离感知 | **2012** | **0.594** | **-0.003** |

**架构升级怎么讲**（核心加分点）：
- GCN 的问题：把晶体图当普通图，**丢弃了原子间距离**——但晶体性质恰恰由几何决定
- 距离感知消息传递（CGCNN 原作思路）：边特征 = **高斯距离展开**（0.5-5.0Å、步长0.25、19维），消息 = h_j ⊙ σ(W·e_ij)，让"远近不同的邻居贡献不同"
- 这就是从"会用PyG"到"懂材料GNN设计"的差别

**必问问题**：
1. "为什么 GNN 没有完胜？" → 2000 结构对 GNN 仍是小数据（CGCNN 原文 ~5000）；带隙数值受组分主导。**0.594 vs 0.591 在 n=2012 的测试集上已无统计显著差异**。
2. "怎么看待轮3比轮2差？" → 距离感知版参数更多（边投影层），1347条时更易过拟合；数据扩到2012后即反超——**架构容量与数据量要匹配**。
3. "训练被环境限制怎么办的？" → checkpoint分段接力训练（每40-70 epoch存档续跑）——工程问题工程解法。

---

## P3 · 候选枚举-筛选闭环（240→200候选→榜单）

**一句话**：枚举-预测-验证三步闭环，magpie 模型筛选电池候选化学式，并量化了模型可信度（与 MP 实测平均偏差 0.913 eV）。这里的“新候选”仅表示当前数据库快照未检索到。

**v2 升级点**：模型从手写特征 RF → magpie HGB；新增"与MP实测偏差"统计。

**必问问题**：
1. "0.913 eV 偏差是不是太大？" → 是的，且偏差主要来自**硫化物被误判**（Li(NiS)₂、Li₂CuS₂ 实测为金属/0 eV，模型预测1.2-1.8）——这再次印证 P2 结论：**组分特征预测不了结构决定的金属性**。闭环里自曝模型局限是特性不是缺陷（诚实评估）。
2. "改进方向？" → 接入 P2 的 GNN 分类器（金属/非金属）做二级过滤，正好组成"P1粗筛+P2精筛"的级联——这是自然的下一步。

---

## 三个项目合起来的叙事线

**P1 证明流程与特征工程 → P2 证明方法学深度（架构设计+迭代实验）→ P3 证明工程化闭环能力**。且三者互相印证：P3 的误差暴露的问题正好是 P2 研究的结论，形成完整科学叙事。

**必须背熟的数字**：
- P1: MAE=0.324, R²=0.714（6772条, magpie 132维）；迭代 0.592→0.640→0.590→0.714
- P1 v3（拆分后）: 分类 ROC-AUC 0.952±0.006；非金属回归 MAE 0.522±0.012；调参仅改善 ≈2%
- MatBench 官方 5 折: expt_gap 0.4459±0.0182（=官方 RF 0.4461）；expt_is_metal 0.9721±0.0024（>官方 RF 0.9356，≈MODNet 0.9739）
- CrabNet（缩减版，官方 5 折）: 0.3953±0.0118（vs 本项目 RF 0.4459；官方完整版 0.3463）
- P2: 旧版 0.594 vs RF 0.591（打平）；**v3 修正版 CGCNN 0.574 vs RF 0.593（GNN 胜出）**；轨迹 0.755→0.606→0.654→0.594→**0.574**
- P3: 240→200 入选；MP对照偏差 0.913 eV（硫化物金属性误判为主）
